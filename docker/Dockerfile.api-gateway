FROM python:3.12-slim

WORKDIR /app

COPY apps/api-gateway/requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

COPY apps/api-gateway/ /app/

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
