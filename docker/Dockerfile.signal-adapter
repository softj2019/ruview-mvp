FROM python:3.12-slim

WORKDIR /app

COPY services/signal-adapter/ /app/

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    python-dotenv \
    numpy \
    scipy \
    websockets \
    supabase

EXPOSE 8001

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
